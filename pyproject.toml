[project]
name = "completed_lab"
version = "0.1.0"
description = "Workshop Lab 1 — a two-source company enrichment crew built with CrewAI"
authors = [{ name = "CrewAI" }]
requires-python = ">=3.10,<3.14"
dependencies = [
    "crewai[tools]==1.15.16",
    "ddgs>=9.0",
]

[project.scripts]
completed_lab = "completed_lab.main:run"
run_crew = "completed_lab.main:run"
train = "completed_lab.main:train"
replay = "completed_lab.main:replay"
test = "completed_lab.main:test"
run_with_trigger = "completed_lab.main:run_with_trigger"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.crewai]
type = "crew"
